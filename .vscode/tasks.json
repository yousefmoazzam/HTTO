{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Tests with coverage",
            "command": "pytest",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "problemMatcher": [],
        }
    ]
}